import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));   // create react element, put inside the html file name root
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


